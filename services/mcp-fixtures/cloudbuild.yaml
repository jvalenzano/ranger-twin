steps:
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - 'gcr.io/$PROJECT_ID/ranger-mcp-fixtures'
      - '-f'
      - 'services/mcp-fixtures/Dockerfile'
      - '.'

images:
  - 'gcr.io/$PROJECT_ID/ranger-mcp-fixtures'
